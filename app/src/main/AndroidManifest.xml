<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.com.progmobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name="android.com.progmobile.Controller.InterrogationReceiver"></receiver>
        <activity android:name=".Controller.ParamActivity"></activity>
        <activity android:name=".Controller.HistoryActivity" />
        <activity android:name=".Controller.InterrogationActivity" />
        <activity android:name=".Controller.AddWordActivity" />
        <activity android:name=".Controller.WordActivity" />
        <activity android:name=".Controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>